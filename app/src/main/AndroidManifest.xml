<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.mychannel.app" >

    <!-- This permission is necessary in order for app to perform network access. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/VideoListTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DetailActivity"
            android:label="@string/title_activity_detail"
            android:parentActivityName=".MainActivity"/>


        <provider
            android:name=".Data.ChannelProvider"
            android:authorities="com.example.android.mychannel.app" />

        <service android:name=".service.MychaneelService"/>

        <receiver android:name=".service.MychaneelService$AlarmReceiver" android:enabled="true">

        </receiver>

    </application>

</manifest>
